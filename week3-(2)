자바스크립트에서 얕은 복사(Shallow Copy)와 깊은 복사(Deep Copy)에 대해 설명해 주세요.
---

얕은 복사는 객체의 참조값(주소 값)을 복사하고, 깊은 복사는 객체의 실제 값을 복사하는 것 이다.
모든 변수는 참조형(Reference type)과 원시형(Primary type)으로 나뉘는데, Primary type을 복사할 경우 새로운 메모리 공간에 "해당 값을 똑같이 갖는 독립적인 값"을 저장하기 떄문에 깊은 복사가 된다.
하지만 Reference Type을 복사 할 경우에, 복사본에 해당 값의 메모리 주소가 복사가 되기 떄문에 얕은 복사가 된다.

즉, 얕은 복사는 해당 값의 "메모리 주소"를 복사하여 저장하는 것이고, 깊은 복사는 해당 값의 "값만" 따로 독립적인 메모리 공간에 저장하는 것 이다. 
따라서 얕은 복사를 할 경우 복사를 당한 쪽과 복사를 한 쪽 중 한가지라도 값이 바뀌면 둘 다 바뀐다. 깊은 복사는 아님. 각각 따로 독립적인 메모리 공간에 값을 저장 한 것이기 떄문에 서로 영향을 받지 않는다.
얕은 복사는 서로 메모리 공간(주소)을 공유하고, 깊은 복사는 공유하지 않고 서로 완전히 다른 독립된 메모리 공간을 갖는다.

참조형 변수에는 list, object 등이 있고, 원시형 변수에는 int, String 등이 있다.
따라서 참조형 변수를 따로 독립적인 메모리 공간을 갖는(따로 움직이고 서로 각각의 수정사항에 영향을 받지 않는) 변수로 쓰기 위해 복사를 해야한다면 따로 for(..~..){list[i] = copy_list[i] ...~...}와 같은 for문을 담고있는 함수를 따로 만들어서
함수를 통해 복사를 하는 방법을 취해야한다.
